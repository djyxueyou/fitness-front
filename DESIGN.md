# FitForge 前端设计规范

## 1. 文档范围

本文档描述 `fitness-front` 当前已经实现的产品结构、视觉语言、组件模式和设计目标，用于约束后续页面设计与前端实现。

设计与实现应以代码库为最终事实来源，主要依据：

- `README.md`
- `src/pages.json`
- `src/styles/`
- `src/components/`
- `src/pages/`
- `src/api/`
- `src/stores/`

注意：README 中“当前数据仍为本地 mock，尚未接入真实后端”的说明已经过期。当前页面通过 `src/api/` 调用训练、计划、模板、动作、日历、修炼等后端接口，设计时必须遵循真实接口的数据结构和状态语义。

## 2. 产品定位

FitForge 是一个基于 `uni-app + Vue 3 + TypeScript` 的多端健身训练产品，目标运行端包括 H5、微信小程序、iOS App 和 Android App。

产品不是单纯的训练表单集合。它围绕以下闭环组织体验：

1. 首页判断用户今天最应该做什么。
2. 用户从周期计划、训练模板或自由训练进入训练。
3. 训练中记录重量、次数、时长、组数和休息。
4. 完成后先进行“修炼结算”，再查看详细训练分析。
5. 通过训练记录、日历、趋势和身体指标理解长期进步。

当前产品核心价值是：让用户更容易开始一次有效训练，并持续看见计划执行、训练表现和修炼成长。

### 2.1 品牌名称

当前对外展示名称以 README、`src/manifest.json` 和关于页中的 **FitForge** 为准。

代码内部仍有 `LIFTLOG_*` 本地存储键，以及默认昵称 `LiftLog User`。这些属于现有技术标识或遗留文案，不应继续扩展为新的界面品牌名称。后续用户可见页面统一使用 FitForge；调整内部存储键时必须考虑已有用户数据迁移。

## 3. 产品原生结构

### 3.1 四个主 Tab

| Tab | 页面 | 核心职责 |
| --- | --- | --- |
| 首页 | `src/pages/home/index.vue` | 根据登录、草稿和当前计划状态，提供今日唯一主行动；展示本周概览与最近训练 |
| 计划 | `src/pages/plan/index.vue` | 管理周期训练安排；启用系统计划或我的计划；查看本周执行进度 |
| 动作库 | `src/pages/exercises/index.vue` | 搜索、筛选、查看、收藏和创建动作 |
| 我的 | `src/pages/profile/index.vue` | 展示个人资料、修炼境界、累计训练数据和个人功能入口 |

### 3.2 核心用户路径

```text
首页今日行动
  -> 继续训练草稿
  -> 开始今日计划
  -> 选择训练模板
  -> 自由训练

训练中
  -> 完成动作与训练组
  -> 保存训练
  -> 修炼结算
  -> 查看详细分析

长期回顾
  -> 训练记录 / 日历
  -> 训练分析
  -> 身体指标与单项趋势
```

### 3.3 信息优先级

页面内容必须遵循以下优先级：

1. **现在应该做什么**：继续草稿、开始今日计划、补训或选择计划。
2. **本次如何完成**：当前动作、当前组、目标值、上次表现、完成状态。
3. **计划执行情况**：本周进度、下一个训练日、计划周期状态。
4. **训练结果与成长**：容量、时长、训练次数、个人纪录、修为。
5. **管理操作**：编辑、复制、删除、停用等放入上下文菜单或二级页面。

## 4. 真实产品术语

界面文案优先复用代码库已有术语，不为相同概念创造新名称。

### 4.1 训练入口

- 今日行动
- 开始今日计划
- 开始下一次
- 查看当前计划
- 选择训练计划
- 选择模板
- 自由训练
- 继续训练
- 训练草稿

### 4.2 训练与分析

- 当前动作
- 完成训练
- 组已完成
- 时长
- 总容量
- 上次表现
- 训练记录
- 训练日历
- 分析
- 查看详细分析

### 4.3 计划

- 系统计划
- 我的计划
- 当前计划
- 本周进度
- 本周剩余
- 下一个训练日
- 本周生效
- 下周生效

计划执行状态来自后端：

- `SCHEDULED`：待生效
- `ACTIVE`：进行中
- `FINISHING`：待收尾
- `COMPLETED`：已完成
- `STOPPED`：已停用
- `REPLACED`：已替换

训练日状态来自后端：

- `NOT_STARTED`：未开始
- `UPCOMING`：待开始
- `TODAY_PENDING`：今日待训练
- `OVERDUE`：待补训
- `COMPLETED`：已完成
- `COMPLETED_EARLY`：提前完成
- `COMPLETED_LATE`：补训完成
- `SKIPPED`：已跳过
- `NOT_APPLICABLE`：未纳入本次计划

前端不得自行推导或改写计划执行状态，应直接展示后端提供的状态和动作类型。

### 4.4 修炼成长

- 修炼结算
- 修为
- 当前境界
- 距离下一阶段
- 炼体期、练气期、筑基期、结丹期、元婴期、化神期

修炼成长是训练完成后的情绪奖励层，不应替代训练数据和计划进度。

## 5. 视觉身份

### 5.1 总体方向

当前视觉语言是深色健身控制台与暖橙能量感的结合：

- 深色背景承载高频记录和数据浏览。
- 橙色用于主行动、当前状态和能量感。
- 绿色用于完成与成功。
- 蓝色、黄色、紫色用于信息分类和修炼境界差异。
- 卡片通过轻微透明度、边框、阴影和有限发光建立层级。

视觉效果必须服务于行动和状态识别。不要让发光、渐变或装饰抢过训练数据。

### 5.2 主题系统

主题由 `src/styles/themes.scss` 和 `src/stores/theme.ts` 管理，支持：

- `system`
- `light`
- `dark`

所有新页面必须使用 CSS 变量，不应在页面中创建另一套主题系统。

#### 深色主题

| Token | 值 | 用途 |
| --- | --- | --- |
| `--app-bg` | `#101115` | 页面背景 |
| `--app-surface` | `#191b21` | 主卡片 |
| `--app-surface-warm` | `#211b18` | 暖色强调卡片 |
| `--app-surface-raised` | `#22252c` | 弹层和浮起表面 |
| `--app-text` | `#f5f6f8` | 主文本 |
| `--app-text-secondary` | `#c3c7cf` | 次级文本 |
| `--app-text-muted` | `#9399a6` | 辅助与说明文本 |
| `--app-border` | `#292d35` | 默认边框 |
| `--app-border-strong` | `#363b45` | 强边框 |
| `--app-accent` | `#ff7138` | 主行动与强调 |
| `--app-success` | `#72c88b` | 完成状态 |
| `--app-warning` | `#e0a54a` | 待处理状态 |
| `--app-danger` | `#f36a6a` | 危险操作 |

#### 浅色主题

| Token | 值 | 用途 |
| --- | --- | --- |
| `--app-bg` | `#f3f6f9` | 页面背景 |
| `--app-surface` | `#ffffff` | 主卡片 |
| `--app-surface-warm` | `#fffaf5` | 暖色强调卡片 |
| `--app-text` | `#162235` | 主文本 |
| `--app-text-muted` | `#8491a3` | 辅助文本 |
| `--app-border` | `#e8edf2` | 默认边框 |
| `--app-accent` | `#ff6418` | 主行动与强调 |
| `--app-success` | `#72bf82` | 完成状态 |
| `--app-danger` | `#e34b4b` | 危险操作 |

### 5.3 兼容中的旧色值

`src/uni.scss` 中仍存在早期深色视觉变量：

- 背景：`#050508`、`#0e0e14`、`#08080c`
- 主橙：`#ff501e`
- 柔和橙：`#ff823c`
- 蓝：`#00c2ff`
- 黄：`#ffd24d`
- 紫：`#bd00ff`

新组件优先使用 `--app-*` 主题变量。只有能量渐变、修炼视觉或已有深色专用页面需要时，才复用旧色值。

### 5.4 字体与层级

全局字体：

```css
font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
```

已形成的字号层级：

| 层级 | 参考字号 | 使用场景 |
| --- | --- | --- |
| 页面主标题 | `42rpx` | AppHeader 页面标题 |
| 强调标题 | `38rpx` 至 `48rpx` | 首页主行动、关键结果 |
| 区块标题 | `30rpx` 至 `32rpx` | 页面分区、卡片主标题 |
| 内容标题 | `25rpx` 至 `28rpx` | 列表项、操作项 |
| 正文/按钮 | `22rpx` 至 `26rpx` | 说明、状态、次级操作 |
| 辅助文字 | `18rpx` 至 `21rpx` | 元数据、时间、单位 |

标题通常使用 `font-weight: 900`，正文和辅助信息降低权重。不得依靠过多字号变化制造层级。

### 5.5 间距、圆角和阴影

- 页面左右内边距：`32rpx`
- 常见内容间距：`12rpx`、`16rpx`、`18rpx`、`20rpx`、`24rpx`
- 区块间距：约 `28rpx`
- 小、中、大圆角：`16rpx`、`24rpx`、`32rpx`
- 主按钮圆角：`28rpx`
- 胶囊状态和筛选项：`999rpx`
- 页面底部必须包含 `env(safe-area-inset-bottom)`

卡片阴影使用 `--app-shadow-card`，重点卡片使用 `--app-shadow-focus`，主行动使用 `--app-shadow-cta`。同一区域避免同时使用强边框、强阴影和强发光。

## 6. 核心组件模式

### 6.1 页面头部

使用 `src/components/app-header/index.vue`：

- 标题必须直接说明当前任务，例如“训练记录”“计划详情”“编辑资料”。
- 副标题只解释页面职责，不重复标题。
- 二级页面显示返回按钮。
- 右侧仅放页面级高频操作。

### 6.2 主行动按钮

使用 `src/components/primary-button/index.vue`：

- 每个视口优先只出现一个橙色主行动。
- 主行动使用明确动词，例如“完成训练”“保存到我的模板”“开始今日计划”。
- 加载状态显示“处理中...”并禁止重复提交。
- 禁用状态必须同时降低视觉权重并阻止点击。

### 6.3 卡片

优先使用：

- `.glass-card`：沉浸式深色卡片、训练与修炼场景。
- `.surface-card`：普通信息卡片和浅色主题。
- `.glass-card--neon`：仅用于当前行动或重要聚焦。

卡片不应层层嵌套。列表项、状态摘要和主要操作必须有清晰职责。

### 6.4 操作面板

使用 `src/components/app-action-sheet/index.vue`：

- 适用于复制、编辑、删除、停用、切换计划等上下文操作。
- 主操作用 `primary`。
- 删除和停用等不可逆或高影响操作用 `danger`。
- 项目标题必须描述结果，说明文字解释影响。

### 6.5 空状态

使用 `src/components/empty-state/index.vue`：

- 标题说明当前状态。
- 描述给出下一步，而不是解释产品功能。
- 存在明确解决路径时提供单一操作。

### 6.6 缩略图与封面

动作、模板、计划训练日和训练记录应统一通过：

- `src/components/exercise-thumbnail/index.vue`
- `src/components/template-cover/index.vue`
- `src/components/training-record-card/index.vue`

封面来自真实动作或记录数据。无图片时根据 `recordType` 使用已有默认视觉，不显示空白图片框。

### 6.7 训练草稿

使用 `src/components/workout-draft-fab/index.vue` 和 `src/components/workout-draft-prompt/index.vue`：

- 草稿浮框固定在右侧，不遮挡主按钮和底部 Tab。
- 展开状态显示“训练草稿”和持续时间，约 2.8 秒后自动折叠。
- 点击后允许继续或删除草稿。
- 只有包含动作且可恢复的训练才显示浮框。

## 7. 页面设计规则

### 7.1 首页

首页的第一任务是回答“今天做什么”。

- 顶部文案由计划推荐、登录状态和草稿状态动态决定。
- 今日行动卡片只能有一个主行动。
- 模板、自由训练、分析是次级快捷入口。
- 本周概览展示训练次数、总容量、时长和周内分布。
- 最近训练最多优先展示两条，完整记录进入训练记录页。

首页不得同时突出多个相同权重的 CTA。

### 7.2 计划

计划页负责长期安排，不负责单次动作编排。

- 顶部当前计划摘要展示本周进度、剩余训练和下一训练日。
- 系统计划和我的计划通过分段控件切换。
- 启用计划前展示“本周生效”和“下周生效”的真实首个训练日。
- 周期、训练日状态和推荐动作由后端计算。
- 管理类操作进入上下文操作面板。

### 7.3 动作库

动作库是可搜索、可筛选的动作目录。

- 支持分类、关键词、器械、难度和记录类型筛选。
- 记录类型只使用：重量次数、自重次数、计时。
- 动作列表展示真实缩略图、动作名、分类和难度。
- 自定义动作必须明确区别于系统动作。

### 7.4 训练中

训练中页面优先保证连续输入效率：

- 顶部始终显示训练来源、计时、完成进度和“完成训练”。
- 当前动作卡片必须高于完整动作列表。
- 组记录字段随动作类型切换。
- 上次表现仅用于对比，模板目标用于本次训练初始值。
- 完成一组后启动休息提示。
- 退出时保存有意义的训练草稿。

训练中页面不得要求用户频繁跳转到二级页面才能完成记录。

### 7.5 训练完成与修炼结算

- 保存成功后先进入“修炼结算”。
- 修为、境界、阶段和增益是情绪焦点。
- 详细训练数据通过“查看详细分析”进入训练总结。
- 无修为增长时仍应明确说明结算规则和原因。

### 7.6 训练记录与日历

训练记录页包含“日历”和“记录”两种浏览模式：

- 月历摘要展示计划数、训练日数和到期完成率。
- 日期只表达三种高层信号：计划安排、已训练、待处理。
- 点击日期后再加载当天计划和训练记录详情。
- 训练记录列表支持按范围浏览，并进入历史详情。

### 7.7 我的

- 顶部统一展示头像、昵称、训练目标、经验和当前境界。
- 当前境界卡片可进入等级详情。
- 连续训练、累计训练、累计容量是固定的个人统计。
- 编辑资料、身体指标、历史记录、模板管理是主要个人工具。
- 会员、收藏、设置和关于属于服务与配置入口。

## 8. 状态与反馈

### 8.1 加载

- 首次加载用页面内状态，不使用空白页面。
- 按钮提交使用局部 loading，并阻止重复点击。
- 训练保存、计划启用、模板复制等高影响操作必须有明确成功或失败反馈。

### 8.2 成功

- 成功状态使用 `--app-success`。
- 已完成状态应优先展示结果或记录入口，不继续显示“开始”。
- Toast 文案描述结果，例如“已启用训练计划”。

### 8.3 待处理

- 待补训、逾期和需要用户决策的状态使用 warning 语义。
- “待处理”必须配套可执行动作，不作为纯装饰标签。

### 8.4 危险操作

- 删除、停用、替换计划必须说明影响。
- 已完成训练记录和计划历史不因切换当前计划而丢失。
- 危险操作使用确认弹层，确认按钮使用 danger 语义。

## 9. 技术与布局约束

- 使用 Vue 3、TypeScript、Pinia 和 uni-app 现有技术栈。
- 不引入新的 UI 框架来替代现有组件。
- 使用 `rpx` 和安全区变量适配多端。
- 所有页面使用自定义导航栏。
- Tab 页面预留底部 TabBar 空间。
- 固定或吸底操作必须避免遮挡内容、草稿浮框和系统安全区。
- 页面主题类来自 `useThemeStore().themeClass`。
- 核心业务状态以 `src/api/` 返回值为准，前端只做展示和交互编排。

## 10. 关键文件

| 文件 | 作用 |
| --- | --- |
| `src/pages.json` | 页面路由、四个主 Tab 和平台页面样式 |
| `src/styles/themes.scss` | 浅色与深色主题变量 |
| `src/styles/global.scss` | 全局布局、卡片、按钮、字段和通用工具类 |
| `src/styles/tab-pages-light.scss` | 主 Tab 浅色主题适配 |
| `src/styles/secondary-pages-light.scss` | 二级页面浅色主题适配 |
| `src/utils/navigation.ts` | 路由常量 |
| `src/pages/home/index.vue` | 今日行动、本周概览和最近训练 |
| `src/pages/plan/index.vue` | 当前计划摘要和计划选择 |
| `src/pages/exercises/index.vue` | 动作目录、搜索与筛选 |
| `src/pages/profile/index.vue` | 个人资料、修炼成长和功能入口 |
| `src/pages/home/workout-active.vue` | 训练中核心交互 |
| `src/pages/home/workout-calendar.vue` | 日历与训练记录聚合浏览 |
| `src/pages/home/cultivation-settlement.vue` | 训练后的修炼结算 |
| `src/components/app-header/index.vue` | 统一页面头部 |
| `src/components/primary-button/index.vue` | 统一主行动按钮 |
| `src/components/app-action-sheet/index.vue` | 统一上下文操作面板 |
| `src/components/workout-draft-fab/index.vue` | 可恢复训练草稿入口 |
| `src/components/exercise-thumbnail/index.vue` | 动作和模板封面基础组件 |
| `src/api/plan.ts` | 计划、训练日和推荐状态契约 |
| `src/api/training.ts` | 训练记录、报告、统计和上次表现契约 |
| `src/api/calendar.ts` | 月历摘要和日期详情契约 |
| `src/api/cultivation.ts` | 境界与修炼结算契约 |
| `src/stores/workout.ts` | 训练执行、草稿、进度和模板目标 |
| `src/stores/plan.ts` | 计划列表、详情缓存和推荐 |
| `src/stores/theme.ts` | 主题模式和 TabBar 主题同步 |

## 11. 后续设计目标

后续页面优化必须围绕以下目标，而不是增加装饰：

1. 用户进入首页后能立即识别唯一主行动。
2. 创建计划、开始训练、记录组数据和查看进步的路径更短。
3. 计划状态、训练日状态和操作文案在所有页面保持一致。
4. 训练中高频操作无需离开当前上下文。
5. 完成训练后既能获得修炼成长反馈，也能理解真实训练进步。
6. 深色和浅色主题均保持可读、可操作和一致。
7. 页面继续复用现有组件和接口，不形成新的平行设计系统。

## 12. 设计验收清单

每次新增或修改页面时检查：

- 页面是否有一个清晰的首要任务？
- 主行动是否唯一、明确且不会被固定元素遮挡？
- 文案是否使用代码库已有术语？
- 状态是否直接来自后端契约？
- 是否复用了现有主题变量和公共组件？
- 浅色和深色主题是否都可读？
- 是否处理了加载、空状态、禁用、成功和失败？
- 固定元素是否适配 TabBar 与安全区？
- 卡片是否承担明确职责，且没有不必要的嵌套？
- 训练、计划、模板和记录中的同一概念是否使用相同文案？
- H5、微信小程序和 App 构建是否仍可通过？
